name: Apply brand selection presentation v93

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-brand-selection-v93.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply v93 patch
        run: python3 scripts/apply-v93-brand-selection-presentation.py

      - name: Validate syntax
        run: |
          node --check app/controllers/workspace-action-service.js
          node --check app/controllers/build-inputs-controller.js
          node --check app/controllers/setup-event-controller-integration.js
          node --check app/rendering-coordinator-integration.js
          node --check app/bootstrap.js
          node --check app.js
          node --check service-worker.js

      - name: Run focused Brand regressions
        run: |
          node tests/brand-selection-regeneration.test.js
          node tests/brand-selection-presentation-v93.test.js
          node tests/setup-bindings-retirement.test.js
          node tests/top-navigation-recovery.test.js
          node tests/workspace-selection-delivery-v93.test.js

      - name: Run protected geometry and presentation regressions
        run: |
          node tests/bottle-orientation-panel.test.js
          node tests/entry-exit-dead-zone-overlay.test.js
          node tests/apl-roller-coverage-default.test.js
          node tests/apl-section-handoff-continuity.test.js

      - name: Commit v93
        run: |
          if git diff --quiet; then
            echo "No v93 changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix Brand selection presentation race v93"
          git push origin HEAD:main
