name: Apply Top View Runtime Load Fix v86
on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/apply-v86-top-view-runtime-load-fix.yml"
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - run: python3 scripts/apply-v86-top-view-runtime-load-fix.py
      - run: |
          node --check app/bottle-orientation-panel-integration.js
          node --check app/bottle-orientation-panel-recovery-integration.js
          node --check app/bootstrap.js
          node --check app/update-manager.js
          node --check service-worker.js
      - run: node tests/bottle-orientation-runtime-load.test.js
      - run: node tests/bottle-orientation-panel.test.js
      - run: node tests/bottle-orientation-mount-recovery.test.js
      - run: node tests/entry-exit-dead-zone-overlay.test.js
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/bottle-orientation-panel-integration.js app/bootstrap.js app/update-manager.js service-worker.js update-manifest.json tests/bottle-orientation-panel.test.js tests/bottle-orientation-mount-recovery.test.js tests/bottle-orientation-runtime-load.test.js
          git commit -m "Fix Bottle Orientation Top View runtime load v86"
          git push origin main
