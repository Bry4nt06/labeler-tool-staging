"use strict";

/**
 * Autocol Services
 *
 * Consolidated from multiple autocol-related features:
 * - v120 (Aug 14, 2026): Terminal boundary — ensures end-of-curve rest command for AUTOCOL machines
 * - v121 (Aug 14, 2026): Printed codebox left edge — renders visual ticks for label and code-box alignment
 *
 * Provides complete autocol machine support: motion planning, servo command sequencing,
 * and printed artwork visualization.
 *
 * Public API:\n *   - ensureAutocolEndCurve() — Add/validate end-of-curve rest command\n *   - applyAutocolTerminalBoundary() — Apply boundary policy to motion program\n *   - activeArtworkGeometry() — Get artwork geometry for current section\n *   - drawArtworkTicks() — Render label and codebox alignment ticks\n */\n(function installAutocolServices(global) {\n  const BUILD_ID = \"autocol-services-consolidated-20260817-1500\";\n  if (global.ServoForgeAutocolServices?.buildId === BUILD_ID) return;\n\n  // ========== v120: Terminal Boundary ==========\n  function runtimeState() {\n    try { return state; }\n    catch { return global.state || {}; }\n  }\n\n  function activeMapSafe() {\n    try { return typeof activeMachineMap === \"function\" ? activeMachineMap() : null; }\n    catch { return null; }\n  }\n\n  function number(value, fallback = null) {\n    const parsed = Number(value);\n    return Number.isFinite(parsed) ? parsed : fallback;\n  }\n\n  function machineContext(rows) {\n    const current = runtimeState();\n    const map = activeMapSafe();\n    return {\n      rows,\n      map,\n      machineType: map?.machineType || map?.name || \"\",\n      machineProfile: current.motionTranslation?.machineProfile\n        || current.motionPlan?.translation?.machineProfile\n        || rows?.find?.((row) => row?.translatedMachineProfile)?.translatedMachineProfile\n        || \"\",\n      applicationMode: current.applicationMode || map?.applicationMode || \"\"\n    };\n  }\n\n  function machineFamily(rows) {\n    const grammar = global.LabelerMachineFamilyGrammarDriver;\n    if (typeof grammar?.resolveFamily === \"function\") {\n      return String(grammar.resolveFamily(machineContext(rows)) || \"DEFAULT\").toUpperCase();\n    }\n    const identity = String(machineContext(rows).machineType || \"\").toUpperCase();\n    return identity.includes(\"AUTOCOL\") ? \"AUTOCOL\" : \"DEFAULT\";\n  }\n\n  const END_CURVE_DEG = 359;\n  const TOLERANCE = 0.5;\n  const TERMINAL_EVENT_ID = \"ME-AUTOCOL-END-CURVE\";\n\n  function isValidEndCurve(row) {\n    return Boolean(row)\n      && Number(row.cmd) === 3\n      && (row.autocolBoundary === \"end-curve\"\n        || (/end\\s*(?:of\\s*)?curve/i.test(String(row.action || \"\")) && row.autocolProfile === true))\n      && Math.abs(number(row.tableAngle, END_CURVE_DEG) - END_CURVE_DEG) <= TOLERANCE;\n  }\n\n  function restCommandName() {\n    return global.LabelerServoCommandDriver?.moveDefinition?.(3)?.name || \"Rest\";\n  }\n\n  function normalizeRows(rows) {\n    return rows.map((row, index) => ({ ...row, hmi: index + 1, plc: index }));\n  }\n\n  function terminalRowFrom(finalRow, index) {\n    return {\n      hmi: index + 1,\n      plc: index,\n      cmd: 3,\n      baseCmd: 3,\n      tableAngle: END_CURVE_DEG,\n      plateAngle: number(finalRow?.plateAngle, 0),\n      action: \"End of curve\",\n      terminalRest: true,\n      activeHold: false,\n      autocolBoundary: \"end-curve\",\n      autocolProfile: true,\n      plannerIntent: \"HOLD\",\n      plannerRequestedCommand: 3,\n      plannerRecommendedCommand: 3,\n      translatedCommandName: restCommandName(),\n      commandTranslated: false,\n      motionSource: \"terminal-end-curve-rest\",\n      motionEventId: TERMINAL_EVENT_ID,\n      motionEventType: \"TERMINAL\",\n      machineGrammarFamily: \"AUTOCOL\",\n      machineGrammarProfile: \"AUTOCOL\"\n    };\n  }\n\n  function ensureAutocolEndCurve(rows) {\n    const source = Array.isArray(rows) ? normalizeRows(rows.map((row) => ({ ...row }))) : [];\n    if (!source.length || machineFamily(source) !== \"AUTOCOL\") return source;\n\n    const finalIndex = source.length - 1;\n    const finalRow = source[finalIndex];\n    if (isValidEndCurve(finalRow)) {\n      source[finalIndex] = {\n        ...finalRow,\n        cmd: 3,\n        baseCmd: 3,\n        tableAngle: END_CURVE_DEG,\n        action: \"End of curve\",\n        terminalRest: true,\n        activeHold: false,\n        autocolBoundary: \"end-curve\",\n        autocolProfile: true,\n        plannerIntent: \"HOLD\",\n        plannerRequestedCommand: 3,\n        plannerRecommendedCommand: 3,\n        translatedCommandName: restCommandName(),\n        commandTranslated: false,\n        motionSource: finalRow.motionSource || \"terminal-end-curve-rest\",\n        motionEventId: finalRow.motionEventId || TERMINAL_EVENT_ID,\n        motionEventType: finalRow.motionEventType || \"TERMINAL\"\n      };\n      return normalizeRows(source);\n    }\n\n    const finalTableAngle = number(finalRow.tableAngle, 0);\n    if (finalTableAngle >= END_CURVE_DEG - TOLERANCE) {\n      // Never create a non-increasing terminal row. Leave genuinely overrun\n      // curves visible to validation instead of hiding a separate geometry fault.\n      return source;\n    }\n\n    source.push(terminalRowFrom(finalRow, source.length));\n    return normalizeRows(source);\n  }\n\n  function syncPlan(plan, rows) {\n    if (!plan || typeof plan !== \"object\") return;\n    const oldSteps = Array.isArray(plan.steps) ? plan.steps : [];\n    plan.steps = rows.map((row, index) => {\n      const previous = oldSteps[index] || {};\n      const eventId = row.motionEventId || previous.eventId || `EV${String(index + 1).padStart(3, \"0\")}`;\n      return {\n        ...previous,\n        index,\n        eventId,\n        mechanicalEventId: row.motionEventId || previous.mechanicalEventId || eventId,\n        eventType: row.motionEventType || previous.eventType || (row.terminalRest ? \"TERMINAL\" : \"GENERAL\"),\n        hmi: row.hmi,\n        plc: row.plc,\n        tableAngle: number(row.tableAngle, 0),\n        plateAngle: number(row.plateAngle, 0),\n        action: String(row.action || \"\"),\n        baseCommand: number(row.baseCmd, number(row.cmd, 3)),\n        requestedCommand: number(row.plannerRequestedCommand, number(row.cmd, 3)),\n        recommendedCommand: number(row.cmd, 3),\n        recommendedCommandName: row.translatedCommandName || `CMD ${row.cmd}`,\n        intent: row.plannerIntent || (Number(row.cmd) === 7 ? \"ROTATE\" : \"HOLD\"),\n        terminal: row.terminalRest === true\n      };\n    });\n\n    if (Array.isArray(plan.events)) {\n      const events = plan.events.slice(0, Math.min(plan.events.length, rows.length));\n      while (events.length < rows.length) {\n        const row = rows[events.length];\n        events.push({\n          id: row.motionEventId || `EV${String(events.length + 1).padStart(3, \"0\")}`,\n          eventId: row.motionEventId || `EV${String(events.length + 1).padStart(3, \"0\")}`,\n          type: row.motionEventType || \"TERMINAL\",\n          eventType: row.motionEventType || \"TERMINAL\",\n          hmi: row.hmi,\n          tableAngle: number(row.tableAngle, 0),\n          plateAngle: number(row.plateAngle, 0),\n          action: String(row.action || \"\")\n        });\n      }\n      plan.events = events;\n    }\n  }\n\n  function syncProgramState(rows) {\n    const current = runtimeState();\n    current.program = rows;\n\n    if (current.motionPlan) {\n      current.motionPlan.rows = rows;\n      current.motionPlan.finalPlateAngle = rows.at(-1)?.plateAngle;\n      current.motionPlan.termination = {\n        ...(current.motionPlan.termination || {}),\n        section: current.motionPlan.termination?.section || \"end-curve\",\n        hmi: rows.at(-1)?.hmi,\n        tableAngle: rows.at(-1)?.tableAngle,\n        command: \"Rest\"\n      };\n      syncPlan(current.motionPlan.planner, rows);\n    }\n\n    if (current.motionTranslation) {\n      current.motionTranslation.rows = rows;\n      syncPlan(current.motionTranslation.plan, rows);\n      current.motionTranslation.commandSummary = rows.reduce((summary, row) => {\n        const command = String(row.cmd);\n        summary[command] = (summary[command] || 0) + 1;\n        return summary;\n      }, {});\n    }\n\n    syncPlan(current.plannerPreview, rows);\n\n    const grammar = global.LabelerMachineFamilyGrammarDriver;\n    if (typeof grammar?.annotateCorrectionChains === \"function\") {\n      const annotated = grammar.annotateCorrectionChains(rows, machineContext(rows));\n      current.program = annotated.rows;\n      if (current.motionPlan) current.motionPlan.rows = current.program;\n      if (current.motionTranslation) current.motionTranslation.rows = current.program;\n      syncPlan(current.motionPlan?.planner, current.program);\n      syncPlan(current.motionTranslation?.plan, current.program);\n      syncPlan(current.plannerPreview, current.program);\n      current.machineFamilyGrammar = {\n        family: annotated.family,\n        rule: annotated.rule,\n        chains: annotated.chains,\n        chainCount: annotated.chains.length\n      };\n    }\n\n    current.machineTerminalPolicy = {\n      family: \"AUTOCOL\",\n      mode: \"END_CURVE\",\n      finalHmi: current.program.at(-1)?.hmi,\n      finalCommand: current.program.at(-1)?.cmd,\n      finalAction: current.program.at(-1)?.action\n    };\n    return current.program;\n  }\n\n  function applyAutocolTerminalBoundary() {\n    const current = runtimeState();\n    if (!Array.isArray(current.program) || !current.program.length) return false;\n    if (machineFamily(current.program) !== \"AUTOCOL\") return false;\n    const nextRows = ensureAutocolEndCurve(current.program);\n    const changed = nextRows.length !== current.program.length\n      || !isValidEndCurve(current.program.at(-1));\n    if (!changed) return false;\n    syncProgramState(nextRows);\n    return true;\n  }\n\n  function installGenerationHook() {\n    let current;\n    try { current = applyGeneratedServoProfile; }\n    catch { current = global.applyGeneratedServoProfile; }\n    if (typeof current !== \"function\") return false;\n    if (current.autocolTerminalBoundaryConsolidated === true) return true;\n\n    const wrapped = function applyGeneratedServoProfileWithAutocolServices(...args) {\n      const output = current.apply(this, args);\n      applyAutocolTerminalBoundary();\n      return output;\n    };\n    wrapped.autocolTerminalBoundaryConsolidated = true;\n    wrapped.previousApplyGeneratedServoProfile = current;\n\n    try { applyGeneratedServoProfile = wrapped; } catch { }\n    global.applyGeneratedServoProfile = wrapped;\n    return true;\n  }\n\n  // ========== v121: Printed Codebox Left Edge ==========\n  function finite(value, fallback = NaN) {\n    if (value === null || value === undefined || value === \"\") return fallback;\n    const parsed = Number(value);\n    return Number.isFinite(parsed) ? parsed : fallback;\n  }\n\n  function activeCodingSection() {\n    try {\n      if (typeof selectedLabelApplicationState !== \"function\") return \"none\";\n      const applications = selectedLabelApplicationState();\n      if (applications?.back) return \"back\";\n      if (applications?.body) return \"body\";\n      if (applications?.neck) return \"neck\";\n    } catch {\n      return \"none\";\n    }\n    return \"none\";\n  }\n\n  function selectedLabel() {\n    try {\n      if (typeof selectedLabelSpec === \"function\") return selectedLabelSpec();\n    } catch { }\n    try {\n      return Array.isArray(state?.labelSpecs)\n        ? state.labelSpecs.find((spec) => spec?.brand === state.selectedBrand) || null\n        : null;\n    } catch {\n      return null;\n    }\n  }\n\n  function selectedBottle() {\n    try {\n      if (typeof selectedBottleSpec === \"function\") return selectedBottleSpec();\n    } catch { }\n    try {\n      return Array.isArray(state?.bottleSpecs)\n        ? state.bottleSpecs.find((spec) => spec?.bottleType === state.selectedBottle) || null\n        : null;\n    } catch {\n      return null;\n    }\n  }\n\n  function sectionLabelLengthMm(section, label) {\n    if (section === \"neck\") return finite(label?.neckLengthMm, NaN);\n    if (section === \"body\") return finite(label?.bodyLengthMm, NaN);\n    if (section === \"back\") return finite(label?.backLengthMm, NaN);\n    return NaN;\n  }\n\n  function sectionCircumferenceMm(section, label, bottle) {\n    if (section === \"neck\") return finite(label?.neckBottomCircumferenceMm, NaN);\n    try {\n      if (typeof bodyCircumference === \"function\") return finite(bodyCircumference(bottle), NaN);\n    } catch { }\n    return NaN;\n  }\n\n  function degreesFromMm(mm, circumferenceMm) {\n    try {\n      if (typeof degFromMm === \"function\") {\n        const converted = finite(degFromMm(mm, circumferenceMm), NaN);\n        if (Number.isFinite(converted)) return converted;\n      }\n    } catch { }\n    if (!Number.isFinite(circumferenceMm) || circumferenceMm <= 0) return NaN;\n    return finite(mm, NaN) / circumferenceMm * 360;\n  }\n\n  function activeArtworkGeometry() {\n    const section = activeCodingSection();\n    if (section === \"none\") return null;\n    const label = selectedLabel();\n    const bottle = selectedBottle();\n    const codeBoxCenterMm = Math.abs(finite(label?.codeBoxCenterMm, NaN));\n    const labelLengthMm = sectionLabelLengthMm(section, label);\n    const circumferenceMm = sectionCircumferenceMm(section, label, bottle);\n    let labelWidthDeg = degreesFromMm(labelLengthMm, circumferenceMm);\n    let codeBoxOffsetDeg = degreesFromMm(codeBoxCenterMm, circumferenceMm);\n\n    if (!Number.isFinite(labelWidthDeg) && typeof global.bottleLabelArcWidthDeg === \"function\") {\n      labelWidthDeg = finite(global.bottleLabelArcWidthDeg(section), NaN);\n    }\n    if (!Number.isFinite(codeBoxOffsetDeg)\n      && Number.isFinite(labelWidthDeg)\n      && Number.isFinite(labelLengthMm)\n      && labelLengthMm > 0\n      && Number.isFinite(codeBoxCenterMm)) {\n      codeBoxOffsetDeg = codeBoxCenterMm / labelLengthMm * labelWidthDeg;\n    }\n\n    const driver = global.LabelerCoderOrientationDriver;\n    if (!driver?.printedLabelLeftEdgeLocalAngle || !driver?.printedCodeBoxLocalAngle) return null;\n    if (![labelWidthDeg, codeBoxOffsetDeg, codeBoxCenterMm].every(Number.isFinite)) return null;\n\n    const leftEdgeAngle = driver.printedLabelLeftEdgeLocalAngle({ section, labelWidthDeg });\n    const codeBoxAngle = driver.printedCodeBoxLocalAngle({\n      section,\n      labelWidthDeg,\n      codeBoxOffsetDeg,\n      inspectionOffsetDeg: 0\n    });\n    if (![leftEdgeAngle, codeBoxAngle].every(Number.isFinite)) return null;\n\n    return {\n      section,\n      codeBoxCenterMm,\n      labelLengthMm,\n      circumferenceMm,\n      labelWidthDeg,\n      codeBoxOffsetDeg,\n      leftEdgeAngle,\n      codeBoxAngle\n    };\n  }\n\n  function point(angle, radius) {\n    const radians = Number(angle) * Math.PI / 180;\n    return {\n      x: Math.cos(radians) * radius,\n      y: Math.sin(radians) * radius\n    };\n  }\n\n  function labelVisibleAt(section, tableAngle) {\n    try {\n      const application = typeof global.bottleLabelApplications === \"function\"\n        ? global.bottleLabelApplications().find((entry) => entry?.section === section)\n        : null;\n      if (!application) return true;\n      return typeof global.bottleHasPassedApplication === \"function\"\n        ? global.bottleHasPassedApplication(tableAngle, application.angle)\n        : true;\n    } catch {\n      return true;\n    }\n  }\n\n  function drawArtworkTicks(add, bottleGroup, tableAngle) {\n    const geometry = activeArtworkGeometry();\n    if (!geometry) return;\n    const visual = global.LabelerBottleVisualRenderer?.indicators?.[geometry.section];\n    if (!visual) return;\n\n    const visible = labelVisibleAt(geometry.section, tableAngle) ? \"inline\" : \"none\";\n    const leftInner = point(geometry.leftEdgeAngle, visual.innerRadius - 0.25);\n    const leftOuter = point(geometry.leftEdgeAngle, visual.outerRadius + 0.9);\n    add(\"line\", {\n      x1: leftInner.x,\n      y1: leftInner.y,\n      x2: leftOuter.x,\n      y2: leftOuter.y,\n      stroke: \"#76f06a\",\n      \"stroke-width\": 0.7,\n      \"stroke-linecap\": \"round\",\n      \"data-bottle-label-left-edge\": geometry.section,\n      \"data-label-left-edge-angle\": geometry.leftEdgeAngle,\n      display: visible,\n      \"pointer-events\": \"none\"\n    }, bottleGroup);\n\n    const codeInner = point(geometry.codeBoxAngle, visual.innerRadius - 0.4);\n    const codeOuter = point(geometry.codeBoxAngle, visual.outerRadius + 1.15);\n    add(\"line\", {\n      x1: codeInner.x,\n      y1: codeInner.y,\n      x2: codeOuter.x,\n      y2: codeOuter.y,\n      stroke: \"#ffd166\",\n      \"stroke-width\": 0.95,\n      \"stroke-linecap\": \"round\",\n      \"data-bottle-code-box-center\": geometry.section,\n      \"data-code-box-center-mm\": geometry.codeBoxCenterMm,\n      \"data-code-box-local-angle\": geometry.codeBoxAngle,\n      display: visible,\n      \"pointer-events\": \"none\"\n    }, bottleGroup);\n  }\n\n  function installRendererHook() {\n    const base = global.drawTopViewBottle;\n    if (typeof base !== \"function\") return false;\n    if (base.autocolServicesConsolidated === true) return true;\n\n    const wrapped = function drawTopViewBottleWithAutocolServices(add, bottleGroup, tableAngle, ...args) {\n      const result = base.call(this, add, bottleGroup, tableAngle, ...args);\n      drawArtworkTicks(add, bottleGroup, tableAngle);\n      return result;\n    };\n    wrapped.autocolServicesConsolidated = true;\n    wrapped.previousDrawTopViewBottle = base;\n    global.drawTopViewBottle = wrapped;\n    return true;\n  }\n\n  // ========== Installation ==========\n  function install() {\n    if (!global.LabelerMachineFamilyGrammarDriver) return false;\n    if (!installGenerationHook()) return false;\n    applyAutocolTerminalBoundary();\n\n    if (!installRendererHook()) {\n      throw new Error(\"Autocol services loaded before the shared bottle renderer.\");\n    }\n\n    global.SERVOFORGE_BUILD_ID = BUILD_ID;\n    global.SERVOFORGE_BUILD_UPDATED_AT = \"Aug 17, 2026 03:30 PM ET\";\n    const banner = global.document?.querySelector?.(\".staging-environment-banner\");\n    if (banner) {\n      const version = String(global.SERVOFORGE_RELEASE_VERSION || \"0.9.10\");\n      banner.textContent = `STAGING ${version} • BUILD ${BUILD_ID} • UPDATED Aug 17, 2026 03:30 PM ET — NOT PRODUCTION`;\n    }\n\n    global.ServoForgeAutocolServices = Object.freeze({\n      installed: true,\n      buildId: BUILD_ID,\n      endCurveDeg: END_CURVE_DEG,\n      terminalEventId: TERMINAL_EVENT_ID,\n      // v120 exports\n      ensureAutocolEndCurve,\n      applyAutocolTerminalBoundary,\n      // v121 exports\n      activeCodingSection,\n      activeArtworkGeometry,\n      drawArtworkTicks,\n      // Feature flags\n      autocolTerminalBoundaryV120: true,\n      printedCodeBoxLeftEdgeV121: true,\n      operatorFacingLeftEdge: true,\n      printedArtworkDirectionInvariant: true\n    });\n    return true;\n  }\n\n  function wait() {\n    if (!install()) global.setTimeout(wait, 25);\n  }\n\n  wait();\n})(typeof window !== \"undefined\" ? window : globalThis);\n
